server:
  port: 9011
spring:
  application:
    name: microservice-order
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_cloud_example?characterEncoding=UTF8
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: rootroot
  jpa:
    show-sql: true
    generate-ddl: true
    database: mysql
feign:
  hystrix:
    enabled: true
